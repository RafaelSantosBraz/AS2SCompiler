public class A {

    public A() {
        // super();
        // int y;
        // String z = "42";
        // int r = x + 3;
        // int p = r * something("a", 3);
        // B b = new B(x);
        // boolean bool = something(2.0, b.xxx);
        // f.g();
        // x = w.y();
        // something(1, j.d);
        // j.something(2, j.d);
        if (x > 3){
            break;
        }
        if (some() < d.j){
            if (a == b){
                
            } else{
                return j.something();
            }
        }
    }

}