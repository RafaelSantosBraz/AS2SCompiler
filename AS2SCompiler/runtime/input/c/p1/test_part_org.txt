
#include <stdlib.h>
#include <stdio.h>
#include <string.h>

int main(int argc, char const *argv[])
{
    char *vet = malloc(sizeof(char) * strlen(argv[1]));
    strcpy(vet, argv[1]);
    int n = atoi(argv[2]);
    printf("%s %d\n", vet, n);
    bubble(vet, n);
    printf("%s\n", vet);
    return 0;
}